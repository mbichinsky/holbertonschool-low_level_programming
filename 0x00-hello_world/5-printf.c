#include <stdio.h>
/**
 * main- printing line using "printf" command
 * Description: prints "with proper grammar, but the
 *              outcome is a piece of art" with a new line at the end
 * Return: returns 0 if no issue
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
